- name: 📢 Send Telegram Notification
  uses: appleboy/telegram-action@master
  with:
    to: ${{ vars.TELEGRAM_CHAT_ID }}  # 必须用vars.前缀引用环境变量
    token: ${{ vars.TELEGRAM_TOKEN }}
    message: "天翼网盘签到结果：${{ job.status }}"
